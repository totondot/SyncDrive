# SyncDrive

Android client with a lightweight Python websocket server for virtual car telemetry.

## Python Server

Install dependencies:

```powershell
python -m pip install -r requirements.txt
```

Run the virtual car server:

```powershell
python virtual_car.py
```
